apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
spec:
  hard:
    requests.cpu: 'req_cpu'
    requests.memory: 'req_mem'
    limits.cpu: 'limit_cpu'
    limits.memory: 'limit_mem'
